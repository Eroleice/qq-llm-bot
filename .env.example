# Required when [llm].provider = "openai-compatible".
OPENAI_API_KEY=replace-with-your-openai-compatible-api-key

# Optional dashboard token. Leave empty to disable dashboard auth.
QQ_LLM_BOT_DASHBOARD_TOKEN=

# Required only when [lexicon].provider = "serper".
WEB_SEARCH_API_KEY=
